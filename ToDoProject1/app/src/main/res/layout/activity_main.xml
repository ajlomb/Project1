<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.alexanderlombardo.todoproject1.MainActivity">

    <TextView
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:text="Greetester Listly Do-hicky Evar!"
        android:textAlignment="center"
        android:paddingTop="25dp"
        android:textSize="20sp"/>

    <ListView
        android:id="@+id/list_view"
        android:layout_below="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:dividerHeight="5dp"
        >
    </ListView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add List"
        android:background="@android:color/holo_green_light"
        android:id="@+id/listbutton"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/list_view"
        android:layout_alignEnd="@+id/list_view"
        android:layout_below="@+id/list_view"
        android:onClick="listAdd"/>

</RelativeLayout>
